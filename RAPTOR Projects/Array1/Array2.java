/**
  * NAME:
  * DATE:
  * FILE:
  * COMMENTS:
  */

public class Array2 extends eecs.Gui
{
   public static void main(String[] args)
   {
      // declare variables
      String raptor_prompt_variable_zzyz = null;
      ?? total = ??;
      ?? count = ??;
      ?? num = ??;
      ??[] nerd = new ??[??];
      
      count = 0;
      total = 0;
      while	(1)
      {
         count = count + 1;
         raptor_prompt_variable_zzyz = "Enter the name of the computer professional: ";
         nerd[count] = get???(raptor_prompt_variable_zzyz);
         if (count == 5)	break;
      }
      raptor_prompt_variable_zzyz = "Enter a number between 1 and 5: ";
      num = get???(raptor_prompt_variable_zzyz);
      printLine("Nerd # " + num + " is " + nerd[num]);
      printLine(nerd[1]);
      printLine(nerd[2]);
      printLine(nerd[3]);
      printLine(nerd[4]);
      printLine(nerd[5]);
   } // close main
} // close Array2
